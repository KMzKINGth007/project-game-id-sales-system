import axios from "axios";
import { useState } from "react";

export default function ShowEditDeleteProduct() {
    const [products, setProducts] = useState([]);

    return (
        <div>
            <h1>สินค้าทั้งหมด</h1>
        </div>
    )
}